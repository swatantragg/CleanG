# Frontend env. Copy to .env to override.
# Leave empty to use the Vite dev proxy ("/api" -> backend).
# For a deployed backend, set the absolute API root, e.g.:
# VITE_API_URL=https://api.example.com/api
VITE_API_URL=
